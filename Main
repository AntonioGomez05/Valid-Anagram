class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        if len(s) != len(t):
            return False
            
        return sorted(s) == sorted(t) // checks to see if they anagrams and returns true



comments: This approach checks if two strings are anagrams using sorting 
